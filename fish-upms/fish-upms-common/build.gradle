apply plugin: 'java-library'

description "用户权限服务公共模块"

dependencies {
    api project(":fish-common:fish-common-core")
    compileOnly project(":fish-common:fish-common-mybatis")

    api "jakarta.validation:jakarta.validation-api"
}
